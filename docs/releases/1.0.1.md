# Forensic CV Manager 1.0.1

## Fixed
- Corrected the Windows build script to invoke PyInstaller with `python -m PyInstaller`, avoiding failures when the user-level Python Scripts directory is not on PATH.

## Compatibility
- No application workflow or database changes.
