# Forensic CV Manager 2.4.1

## Fixed
- Corrected startup compatibility between `app.py` and `SplashScreen` after the 2.4.0 source synchronization.
- Added the `theme_name`, `set_status()`, and `close()` compatibility expected by the current startup sequence.
- Updated the PDF preview module to use `import pymupdf as fitz`, removing the deprecated `fitz` import warning while keeping the existing preview API intact.
